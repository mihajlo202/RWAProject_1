import { Router } from "../../router";

export class MainPage {
    _router: Router;

    constructor() {
        this._router = new Router();
    }

    drawMainPage(container: HTMLDivElement) {

    }
}